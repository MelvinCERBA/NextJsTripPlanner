import { TripSearchBar } from './TripSearchBar';

export default {
  title: 'Components/TripSearchBar',
  component: TripSearchBar,
};

const Template = (args) => <TripSearchBar {...args} />;

export const Default = Template.bind({});